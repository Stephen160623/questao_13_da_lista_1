# Atribuição de variáveis
   
# Entrada de dados

r = float(input("Qual a renda do consumidor: "))
p = float(input("Qual o preço do bem: "))

#Processamento de dados

q = r/p

# Saída de dados

print("A quantidade demandada é: "  + str(q))